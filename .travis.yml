# Disable sudo to speed up the build
sudo: false
dist: xenial
language: python
python:
  - "3.7"
install:
  - pip install codecov
  - pip install -r requirements.txt
script:
  - pytest --cov=truegaze/
after_success:
  - codecov